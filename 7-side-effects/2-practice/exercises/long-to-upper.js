/**
 * Upper-cases all string over a specific length in an array.
 * It returns a new array and has no side-effects.
 *
 * @param {string[]} [strings=[]] - An array of strings to upper-case.
 * @param {number} [long=0] - The minimum length of string to upper-case.
 *
 * @returns {string[]} An new array containing (possibly) upper-cased strings.
 */
export const longToUpper = () => {};
